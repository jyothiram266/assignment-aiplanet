apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: default
  labels:
    app: llm-gateway
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-gateway
